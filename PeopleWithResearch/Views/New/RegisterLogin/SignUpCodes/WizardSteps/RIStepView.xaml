<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:syncfusion="clr-namespace:Syncfusion.Maui.ListView;assembly=Syncfusion.Maui.ListView"
    x:Class="PeopleWithResearch.Views.New.RegisterLogin.SignUpCodes.RIStepView">
    <Grid Padding="20,10,20,16">
        <VerticalStackLayout Spacing="0">

            <Label
                Text="{Binding StepTitle}"
                FontFamily="HankenGroteskBold"
                FontSize="18"
                TextColor="#031926"
                HorizontalOptions="Start"
                Margin="0,10,0,0" />

            <Label
                Text="{Binding StepSubtitle}"
                FontFamily="HankenGroteskRegular"
                FontSize="13"
                TextColor="#777777"
                HorizontalOptions="Start"
                LineBreakMode="WordWrap"
                Margin="0,5,0,0" />

            <syncfusion:SfListView
                x:Name="coughlist"
                Margin="0,10,0,0"
                AutoFitMode="Height"
                BackgroundColor="Transparent"
                IsScrollingEnabled="False"
                Orientation="Vertical"
                ScrollBarVisibility="Never"
                SelectionBackground="Transparent"
                SelectionGesture="Tap"
                SelectionMode="Single"
                ItemsSource="{Binding RiOptions}"
                SelectedItem="{Binding SelectedRiOption, Mode=TwoWay}">
                <syncfusion:SfListView.ItemsLayout>
                    <syncfusion:GridLayout SpanCount="1" />
                </syncfusion:SfListView.ItemsLayout>
                <syncfusion:SfListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Border Margin="5,5,5,5" Padding="20" BackgroundColor="Transparent"
                                    Stroke="LightGray" StrokeShape="RoundRectangle 10">
                                <Label FontFamily="HankenGroteskRegular" FontSize="12"
                                       Text="{Binding Text}" TextColor="Gray"
                                       InputTransparent="True" VerticalOptions="Center" />
                            </Border>
                        </ViewCell>
                    </DataTemplate>
                </syncfusion:SfListView.ItemTemplate>
                <syncfusion:SfListView.SelectedItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Border Margin="5,5,5,5" Padding="20" BackgroundColor="#d8e9fa"
                                    Stroke="#009fe3" StrokeShape="RoundRectangle 10" StrokeThickness="2">
                                <Label FontAttributes="Bold" FontFamily="HankenGroteskBold" FontSize="12"
                                       Text="{Binding Text}" TextColor="#031926"
                                       InputTransparent="True" VerticalOptions="Center" />
                            </Border>
                        </ViewCell>
                    </DataTemplate>
                </syncfusion:SfListView.SelectedItemTemplate>
            </syncfusion:SfListView>

            <Label
                Text="{Binding RiStackErrorMessage}"
                IsVisible="{Binding RiStackError}"
                FontFamily="HankenGroteskBold"
                FontSize="10"
                TextColor="Red"
                HorizontalOptions="End"
                Margin="0,4,0,0" />

        </VerticalStackLayout>
    </Grid>
</ContentView>
